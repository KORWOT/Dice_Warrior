# AGENTS.md — Codex Unity 프로젝트 룰

> v0.5 · 2026-09-07 · 기능 중심 폴더·1:1 요약 유지 + 선택적 다중 에이전트

## 1. 역할과 활성 기준
- Codex만 사용하되 실행 에이전트 수를 하나로 강제하지 않는다. 메인 Codex가 범위·공통 계약·통합·최종 판정을 책임지며 Claude/ORCA의 선행 작업을 요구하지 않는다.
- 단일·다중 실행은 작업에 맞게 선택한다. 승인 범위의 조사·검토·독립 구현을 보조 Codex에 위임할 수 있다. 위임별 입력·산출물·쓰기 경계와 유한한 동시/누적 위임 상한을 PLAN에 정하고, 인원만 늘리기 위한 중복·재귀 위임은 하지 않는다.
- 같은 파일은 동시에 한 작성자만 수정한다. 공통 계약·SO·색인·PLAN/REPORT의 갱신 담당을 정하고 동일 프로젝트의 Unity 실행은 직렬화한다. 세부 조건은 운영 기준 2.1절을 따른다.
- 승인된 Design·Architecture·PLAN과 보호 규칙은 유지한다. GDD의 확정 방향·프로토타입 제안·추후 결정을 구분한다.
- 상세 운영은 [CODEX_WORKFLOW.md](Docs/AI/CODEX_WORKFLOW.md)의 해당 절만 읽는다. 미결정은 현재 Task의 필수 계약을 막는 경우에만 차단한다.

## 2. 시작과 범위
- `DOCUMENT` 또는 `IMPLEMENT`를 정하고 `Docs/Plans/<TASK>_PLAN.md`에 계약·진행을 기록한다. 기존 PLAN을 읽지 않고 구현하지 않는다.
- 목표·필수 AC·비목표·고정 대상·허용 경로·필수 검증·반복 상한을 정한다. 구현 시 대응 CodeMap·직접 영향받는 관계·색인 항목도 범위에 포함한다.
- Git 기준 commit과 기존 staged/unstaged/untracked 변경을 기록한다. 사용자 변경과 대상이 겹치면 임의 편집·reset·stash하지 않는다.
- 필요한 원문·코드 추가 확인은 허용한다. 전체 재탐색, 새 문서 자동 추가, 무관한 정비는 하지 않는다.

## 3. 기능 폴더와 코드 탐색
- 새 기능은 `Assets/_Project/Features/<Feature>/` 아래에 모으고, 실제 필요한 하위 폴더만 만든다. 기존 파일 일괄 이동은 별도 승인 없이 하지 않는다.
- 폴더·파일명은 역할이 드러나게 짓는다. 기능별 의미는 한국어 색인으로 설명한다. 기준은 [PROJECT_STRUCTURE.md](Docs/AI/PROJECT_STRUCTURE.md)를 따른다.
- 검수 전 해당 기능 색인과 대상 스크립트 요약을 먼저 읽는다. 이후 실제 원본·직접 참조·관련 검증 증거로 확인한다. 문서만으로 코드 검수를 완료하지 않는다.
- 새로 작성하거나 수정하는 프로젝트 소유 C#은 `Docs/CodeMap/<원본의 프로젝트 상대 경로>.md`에 1:1 요약을 작성·동기화한다. 예: `Foo.cs` → 같은 상대 경로의 `Foo.cs.md`.
- 요약에는 역할·핵심 동작·입출력·사용하는 대상·사용하는 쪽·관계 근거·상태/수명·검수 주의점을 적는다. 모르는 참조는 만들지 않는다.
- 추가·이동·삭제·책임·계약·참조 변화는 해당 요약과 관련 색인/직접 관계만 반영한다. 범위와 양식은 [CODE_DOCUMENTATION.md](Docs/AI/CODE_DOCUMENTATION.md)를 따른다.

## 4. 구현과 완료
- `DOCUMENT`: 해당 문서의 계약·책임·실패 조건·검증 방법이 명확하고 현재 범위의 필수 모순·미결정이 없으면 완료다.
- 문서만 바꿀 때 CodeMap 동기화·Unity 검증을 자동 요구하지 않는다. 단, 요청 자체가 CodeMap 작성/보정이면 지정 문서는 작성한다.
- `IMPLEMENT`: 필수 AC·실제 검증 PASS, 새 직접 회귀와 명백한 구조·의존성 결함 없음, 해당 코드 요약·색인·REPORT가 완료 기준이다.
- 가장 작은 일관된 변경으로 직접 원인을 해결한다. 기존 경계를 존중하고 SOLID를 이유로 미래 대비용 추상화·리팩터링을 추가하지 않는다.
- 기존 singleton·탐색 API의 존재만으로 사용을 확대하지 않는다. 새 순환 의존성·숨은 전역 결합을 만들지 않는다.
- 미실행 검증은 PASS가 아니다. 테스트 인프라 부재와 검증 비적용을 구분하고 필수 미검증은 대기·차단으로 남긴다.

## 5. 검토와 종료
- 최초 검토는 Task/문서당 한 검토 묶음이다. 여러 검토자는 지정된 범위를 나눠 확인하며 메인은 근거 확인·중복 제거로 통합한다. 구현 CodeMap과 보조 작업에 별도 리뷰/수정 예산을 만들지 않는다.
- 필수 Finding에 ID·근거·영향·해결 조건을 적고, 이후에는 기존 Finding·지정 검증·직접 회귀·범위 이탈만 확인한다. 일반 개선점은 FOLLOW_UP으로 둔다.
- 최초 산출물 검증 이후 수정·재검증은 Task/문서당 기본 총 2회다. 단계·세션·이름 변경으로 누적 횟수를 초기화하지 않는다.
- 새 필수 결함은 숨기지 않는다. 남은 예산 안에서 해결하거나 차단한다. 기준을 낮추거나 미해결 문제를 승인하지 않는다.
- 완료 문서는 새 계약·구체적 모순·회귀 근거 없이 재개방하지 않는다. 성공 또는 차단 조건에 도달하면 종료한다.

## 6. 자산·Git·보고
- Scene/Prefab 쓰기는 PLAN에 파일 단위로 허용된 경우만 한다. 아니면 ASSET_HANDOFF로 넘기며 코드로 연결을 우회하지 않는다.
- 허용 자산 수정은 Editor API/브리지로 한다. 기존 .meta/GUID·직렬화 계약을 보호하고 신규 자산의 .meta를 함께 관리한다.
- Editor 상태·검증 채널을 확인한다. 같은 프로젝트 중복 batchmode·잠금 삭제·자동 hook/MCP/전역 설정 변경은 하지 않는다.
- 사람과 Codex 모두 정확한 파일만 staging한다. `git add -A`·`git commit -a`는 금지하며 기존 staged 변경을 포함하지 않는다.
- commit·branch 변경은 승인 정책 안에서만 한다. WIP는 완료가 아니다. HANDOFF 중 동일 파일을 동시에 편집하지 않는다.
- package lock·프로젝트 설정의 예기치 않은 변경을 숨기지 않는다. 필수 증거에 영향을 주면 재확인 전 승인하지 않는다.
- `Docs/Reports/<TASK>_REPORT.md`에 상태·실제 증거·요약 문서/관계 갱신·미검증·횟수·남은 Finding을 기록한다. 예산 소진은 성공이 아니다.
- 필요할 때만 [문서 Goal](Docs/AI/CODEX_DOCUMENT_GOAL.md), [Fate Dice 구현 Goal](Docs/AI/FATE_DICE_IMPLEMENTATION_GOAL.md), [작업 양식](Docs/AI/CODEX_TASK_TEMPLATE.md), [요약 양식](Docs/AI/CODEMAP_TEMPLATE.md)을 읽는다. 단순 질의응답에는 새 작업 문서를 요구하지 않는다.
